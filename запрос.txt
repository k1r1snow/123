-- Предметы
DROP TABLE IF EXISTS classes;

CREATE TABLE classes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(128)
);

INSERT INTO classes (name) VALUES
('СУДБ'), ('пайтон он веб'), ('оп на тайпскрипт');

-- Группы
DROP TABLE IF EXISTS student_group;

CREATE TABLE student_group (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(32)
);

INSERT INTO student_group (name) VALUES
('ВР401'), ('ВР402');

-- Студенты
DROP TABLE IF EXISTS student;

CREATE TABLE student (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(32),
    surname VARCHAR(32),
    age INT,
    group_id INT,
    FOREIGN KEY (group_id) REFERENCES student_group(id) ON DELETE CASCADE
);

INSERT INTO student (name, surname, age, group_id) VALUES
('биба', 'бобов', 228, 1), ('лелик', 'боликов', 67, 1);

-- Преподы
DROP TABLE IF EXISTS teacher;

CREATE TABLE teacher (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(32),
    surname VARCHAR(32),
    age INT,
    group_id INT
);

INSERT INTO teacher (name, surname, age) VALUES
('ярик', 'некрасов', 228), ('вупсень', 'пупсенов', 67);
